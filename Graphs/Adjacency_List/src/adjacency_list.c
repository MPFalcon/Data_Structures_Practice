#include "adjacency_list.h"

int setup_program(uint64_t size)
{
    int err = FAILURE;

    printf("\n\nProgram start with size: %lu\n\n", size);

    err = SUCCESS;

    return err;
}

/*** EOF ***/
